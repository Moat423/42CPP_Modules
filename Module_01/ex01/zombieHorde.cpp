
#include "Zombie.hpp"


//allocates N zombies in the heap, and returns a pointer to the first one
Zombie* zombieHorde( int N, std::string name )
{
	Zombie	*horde;
	int		i = 0;

	horde = newZombie(name);
	while (i < N)
	{
	}
}
